export enum FeatureEvents {
  STARTED = 'Feature started',
  LOADED = 'Feature loaded',
  RERENDERED = 'Feature rerendered',
  CANCELLED = 'Feature cancelled',
  ERRORED = 'Feature errored',
}

export enum ErrorBoundaryTypes {
  DEFAULT = 'default',
  SILENT = 'silent',
  CUSTOM = 'custom',
}
